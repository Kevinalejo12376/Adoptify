services:
    n8n:
        image: n8nio/n8n:latest
        container_name: adoptify-n8n

        ports:
          - "5678:5678"

        volumes:
          - ./data:/home/node/.n8n

        restart: unless-stopped

        env_file:
          - .env
